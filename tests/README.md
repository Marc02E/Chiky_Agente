# Test Strategy

Required layers: unit, integration, contract, performance, security and E2E. Coverage targets are governed by AG-CONST-024. Tests MUST map to requirements/specifications and MUST NOT contain production secrets.
